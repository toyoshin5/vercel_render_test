services:
  - type: web
    name: fastapi
    runtime: python
    repo: https://github.com/your-username/your-repo-name # ご自身のGitHubリポジトリのURLに書き換えてください
    branch: main
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
